import { MovieTimeline } from "@/components/rare/movie-timeline/movie-timeline";

export const metadata = { title: "Movie Timeline" };

export default function TimelinePage() {
  return <MovieTimeline />;
}
